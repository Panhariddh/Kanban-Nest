# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Task {
  id: ID!
  title: String!
  description: String
  status: TaskStatus!
  board: Board!
}

enum TaskStatus {
  TODO
  IN_PROGRESS
  DONE
}

type Board {
  id: ID!
  title: String!
  owner: UserModel!
  tasks: [Task!]
}

type UserModel {
  id: ID!
  name: String!
  email: String!
  avatar: String
}

type Query {
  me: UserModel!
  boards: [Board!]!
}

type Mutation {
  createBoard(title: String!): Board!
  updateBoard(id: Float!, title: String!): Board!
  deleteBoard(id: Float!): Boolean!
  createTask(title: String!, boardId: Float!): Task!
  moveTask(taskId: Float!, status: TaskStatus!): Boolean!
  updateTask(id: Float!, title: String, description: String): Task!
  deleteTask(id: Float!): Boolean!
}

type Subscription {
  taskUpdated(boardId: Float!): Task!
}